services:
  api:
    build: .
    ports:
      - "8000:8000"
    volumes:
      - .:/app
    command: >
      sh -c "python -m moscow_housing.prepare_data &&
             python -m moscow_housing.train_cp2 &&
             uvicorn moscow_housing.api:app --host 0.0.0.0 --port 8000"
